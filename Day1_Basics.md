1. `pwd`: **P**rint **W**orking **D**irectory (현재 내가 있는 위치 확인)
2. `ls`: **L**i**s**t (현재 폴더에 어떤 파일이 있는지 목록 보기)
   - `ls -l`: 상세 정보 보기
   - `ls -a`: 숨겨진 파일까지 다 보기
3. `cd`: **C**hange **D**irectory (다른 폴더로 이동하기)
